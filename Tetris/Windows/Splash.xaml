<Window x:Class="Tetris.Windows.Splash"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:Tetris.Windows"
        xmlns:controls="clr-namespace:Tetris.Controls"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
         Background="Transparent"
        Cursor="Wait"
        Title="Splash" Height="400" Width="640">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
        CaptionHeight="32"
        ResizeBorderThickness="6"
        CornerRadius="0"
        GlassFrameThickness="0"
        UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>






    <Grid>

        <!-- تصویر پس زمینه -->
        <Border CornerRadius="5">
            <Border.Background>
                <ImageBrush x:Name="backgroundImage"
                        Stretch="Fill"/>
            </Border.Background>

            <TextBlock x:Name="lblVersionWarning" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="Black" FontSize="18" Text="This version is under development and experimental.">
                
            </TextBlock>
        </Border>
        <controls:SplashProgressBar
            x:Name="loadingProgress"
    Height="8"
    VerticalAlignment="Bottom"
    HorizontalAlignment="Stretch"
    Margin="0,0,0,-1"
            
    Value="0"/>

    </Grid>
</Window>
